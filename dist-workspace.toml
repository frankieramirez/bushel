[workspace]
members = ["cargo:."]

[dist]
cargo-dist-version = "0.32.0"
ci = "github"
installers = ["shell", "homebrew"]
targets = ["aarch64-apple-darwin"]
hosting = "github"
tap = "frankieramirez/homebrew-tap"
publish-jobs = ["homebrew"]
post-announce-jobs = ["./deploy-site", "./publish-crates"]

[[dist.extra-artifacts]]
artifacts = ["docs.json"]
build = ["cargo", "run", "--example", "docs-json", "--", "--out", "docs.json"]

[[dist.extra-artifacts]]
artifacts = ["bushel.1", "bushel.bash", "bushel.zsh", "bushel.fish"]
build = ["cargo", "run", "--example", "dist-assets", "--", "--out-dir", "."]
